[project]
name = "bidsif"
version = "0.1.0"
description = "A library for BIDSifing EEG data"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["uv_build>=0.11.26,<0.12"]
build-backend = "uv_build"
